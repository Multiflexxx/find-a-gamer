import { GameValidatorDirective } from './game-validator.directive';

describe('GameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
